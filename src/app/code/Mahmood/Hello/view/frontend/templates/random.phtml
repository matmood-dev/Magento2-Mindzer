<?php
$product = $block->getRandomProduct();
if ($product && $product->getId()):
?>

<div class="random-product" style="border:1px solid #ccc; padding:20px; margin:40px 0; text-align:center;">
    <h2>🔥 Random Pick for You</h2>
    <h3><?= $product->getName(); ?></h3>
    <p><?= $product->getPrice(); ?> <?= $block->escapeHtml($block->getCurrentCurrencyCode()) ?></p>
    <a href="<?= $product->getProductUrl(); ?>" style="background:#008aad; color:#fff; padding:10px 20px; border-radius:6px;">View Product</a>
</div>

<?php endif; ?>

